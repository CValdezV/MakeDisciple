<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cwu.makedisciple">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TableOfContents">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".About"
            android:parentActivityName=".TableOfContents">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TableOfContents" />
        </activity>
        <activity
            android:name=".BookFrame"
            android:parentActivityName=".TableOfContents">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TableOfContents" />
        </activity>
        <activity
            android:name=".CalendarLog"
            android:parentActivityName=".TableOfContents">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TableOfContents" />
        </activity>
        <activity
            android:name=".NoteTaking"
            android:parentActivityName=".TableOfContents">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TableOfContents" />
        </activity>
        <activity android:name=".Abbreviations"></activity>
    </application>

</manifest>